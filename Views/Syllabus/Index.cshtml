@model List<Syllabus>
@{
    ViewData["Title"] = "Qu?n lý ?? c??ng";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>@ViewData["Title"]</h2>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-lg"></i> Thêm m?i
    </a>
</div>

<table class="table table-bordered table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Môn h?c</th>
            <th>Phiên b?n</th>
            <th>Ngôn ng?</th>
            <th>Ng??i phê duy?t</th>
            <th>Ngày ?i?u ch?nh</th>
            <th>Ngày t?o</th>
            <th style="width: 180px">Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.CourseName</td>
                <td>@item.Version</td>
                <td>@item.Language</td>
                <td>@item.ApprovedBy</td>
                <td>@item.AdjustmentDate?.ToString("dd/MM/yyyy")</td>
                <td>@item.CreatedAt?.ToString("dd/MM/yyyy HH:mm")</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">S?a</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@if (!Model.Any())
{
    <div class="alert alert-info">Ch?a có d? li?u ?? c??ng nào.</div>
}
