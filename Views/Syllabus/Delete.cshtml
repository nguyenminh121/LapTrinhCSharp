@model Syllabus
@{
    ViewData["Title"] = "Xóa ?? c??ng";
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning">
    <h5>B?n có ch?c ch?n mu?n xóa ?? c??ng này?</h5>
</div>

<div class="card" style="max-width: 500px;">
    <div class="card-body">
        <dl class="row mb-0">
            <dt class="col-sm-4">ID</dt>
            <dd class="col-sm-8">@Model.Id</dd>
            <dt class="col-sm-4">Môn h?c</dt>
            <dd class="col-sm-8">@Model.CourseName</dd>
            <dt class="col-sm-4">Phiên b?n</dt>
            <dd class="col-sm-8">@Model.Version</dd>
            <dt class="col-sm-4">Ngôn ng?</dt>
            <dd class="col-sm-8">@Model.Language</dd>
            <dt class="col-sm-4">Ng??i phê duy?t</dt>
            <dd class="col-sm-8">@Model.ApprovedBy</dd>
        </dl>
    </div>
</div>

<form asp-action="Delete" method="post" class="mt-3">
    <input type="hidden" name="id" value="@Model.Id" />
    <button type="submit" class="btn btn-danger">Xác nh?n xóa</button>
    <a asp-action="Index" class="btn btn-secondary">H?y</a>
</form>
