@model Major
@{
    ViewData["Title"] = "Xóa Ngành h?c";
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning">
    <h5>B?n có ch?c ch?n mu?n xóa ngành h?c này?</h5>
</div>

<div class="card" style="max-width: 500px;">
    <div class="card-body">
        <dl class="row mb-0">
            <dt class="col-sm-4">ID</dt>
            <dd class="col-sm-8">@Model.Id</dd>
            <dt class="col-sm-4">Tên ngành</dt>
            <dd class="col-sm-8">@Model.Name</dd>
            <dt class="col-sm-4">Khoa</dt>
            <dd class="col-sm-8">@Model.FacultyName</dd>
            <dt class="col-sm-4">Trình ??</dt>
            <dd class="col-sm-8">@Model.DegreeLevel</dd>
            <dt class="col-sm-4">S? TC yêu c?u</dt>
            <dd class="col-sm-8">@Model.RequiredCredits</dd>
        </dl>
    </div>
</div>

<form asp-action="Delete" method="post" class="mt-3">
    <input type="hidden" name="id" value="@Model.Id" />
    <button type="submit" class="btn btn-danger">Xác nh?n xóa</button>
    <a asp-action="Index" class="btn btn-secondary">H?y</a>
</form>
